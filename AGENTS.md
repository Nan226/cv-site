# CV-Site 协作规则

本文件用于 Codex、Claude Code、VS Code 内的 AI 助手及人工开发者之间交接项目。
本文件的目标是统一约束不同助手的执行方式，避免因为聊天上下文不共享而出现误判、重复劳动或覆盖式修改。

## 沟通与执行

- 与用户沟通、编写实施计划和汇报改动时使用中文。
- 开始工作前先阅读本文件、`Logs/` 中最新的日志，以及与任务相关的现有代码。
- 用户要求分步骤推进。每完成一个主要步骤后先汇报结果并等待用户确认，不要一次完成所有后续步骤。
- 对需求存在关键歧义时先询问用户；可以从现有文件或图片中确认的信息，不重复询问。
- `AGENTS.md` 是 Codex 与 Claude Code 的共享约束文件；当代码现状或协作方式发生变化时，应先更新本文件或日志，再继续实施。

## 日志要求

- 每次进行文件创建、修改、删除、命令执行、设计决策或验证，都要记录到 `Logs/`。
- 日志文件按日期命名：`Logs/YYYY-MM-DD.md`；当天文件存在时追加，不重复创建。
- 日志至少写明：时间或日期、执行者、操作、涉及文件、结果，以及仍待确认的事项。
- 开始新会话时先读最新日志；结束一个步骤前先补全日志。
- 聊天上下文不视为项目记录。重要需求、决定和进度必须落盘到日志或本文件。

## 多助手协作

- Codex 与 Claude Code 不共享聊天会话，只通过工作区文件、日志和版本控制状态交接。
- 不得假设自己知道另一个助手尚未写入文件的计划或推理。
- 修改前检查现有文件，不覆盖或回退用户及其他助手的改动。
- 发现不属于当前任务的改动时保留原状；若与当前任务冲突，先记录并向用户说明。
- 交接时在当天日志中注明已完成内容、未完成内容、下一步和已知风险。

## 当前项目基线

- 当前代码基线已经是一个单页多 section 作品集，而非仅有 HOME 的空白项目。
- 当前已落地 section 为：`HOME`、`About Me`、`Internship`、`Projects`、`Skills & Learning`、`Me & My Friends`（内部 id 仍为 `travel-memory`）。
- 导航中的 `Skills & Learning` 为单一入口并滚动到合并 section；`Me & My Friends` 入口滚动到第六屏翻牌游戏。
- 后续新需求默认基于当前真实代码状态推进，不再以“只有 HOME 页面”的旧阶段描述作为唯一依据。
- 若用户要求聚焦某一 section，只修改该 section 及其必要联动，不顺手重做其他页面。

## 当前页面基线

- 技术栈为纯 HTML、CSS 和原生 JavaScript，除非用户明确同意更换。
- 页面仍以白色为主，融合非常淡的蓝色、粉色或紫粉色光晕，避免明显分隔色条。
- 顶部导航为 `About Me`、`Internship`、`Projects`、`Skills & Learning`、`Me & My Friends`；左侧为手绘房屋图标首页按钮，并保留文字撕裂悬浮效果基线。
- 除非用户明确要求改架构，否则继续沿用现有单页滚动结构与 section 组织方式。

### HOME

- 视觉以 `images/Home参考.png` 和 `images/HOME/` 下的细节参考图为主。
- 主标题为 `YE NAN`，副标题为 `Builder · Dreamer · Braver`。
- Home 静态视觉使用与 Projects 一致的插画纸张语言：深色手绘描边、奶油纸张底、粉蓝绿黄贴纸色块、小圆角和轻微错位硬阴影；不改变 3D 人物与彩蛋交互。
- Home、About Me、Internship 背景共用粉蓝绿黄纸带飘落动画；进入 Projects 及后续 section 后自动隐藏。
- 人物左右各排列五个标签。
- 中间人物采用全身 3D 角色，身体、头部和眼神轻微跟随鼠标，并支持头、脸、肚子、手、脚、肩、腿和身体等分部位点击反馈。
- 保留当前标签撕碎彩蛋、暗场切换和拉绳恢复这条交互链路，除非用户明确要求删除或重做。
- 暗场中 3D 人物必须完整保持正常亮度，并带紫色轮廓光；暗幕只压暗人物以外的环境。

### About Me

- 当前基线为：时间线 + 环绕轮播卡片。
- 时间线与轮播卡片已统一为奶油纸张、深色描边、扁平粉蓝绿黄标签和硬阴影；桌面 3D 环绕、移动端横滑及自动旋转逻辑不变。
- 卡片图片与正文比例已按屏幕高度响应式分配：较矮桌面以完整容纳正文为先，常规高桌面图片约占三分之二；移动端继续使用紧凑横滑比例。标签、标题和操作按钮必须完整留在卡片边界内。
- 卡片包含基础信息、教育和实习节点，并保留 Resume 弹框与跳转 Internship 的入口。
- 可见 UI 文案以英文为主。

### Internship

- 当前基线为：三张实习卡片直接展示并可点击展开详情。
- 主卡片与详情视图已统一为 Projects 插画窗口的视觉语言：深色手绘描边、奶油纸张底、粉蓝绿扁平色块、轻微错位硬阴影和小圆角；不再使用紫蓝玻璃渐变卡片。
- 详情页的 `Back to Journey` 位于左侧预览卡左下角，不放在右侧详情正文中；主脚本与兜底脚本需保持一致。
- 代码中保留了早期开场图/撕裂相关样式痕迹，但当前实际状态以已落地代码为准。
- `js/internship-boot.js` 是独立兜底脚本，修改 Internship 时需同时检查它与 `js/main.js` 的一致性。

### Projects

- 当前基线为：Matter.js 2D 物理 + DOM/CSS 手绘球体；桌面生成约 52 个球，移动端约 30 个球，从 section 顶部落下、碰撞并堆叠在底部。
- `js/projects-orbs.js` 是当前主流程模块，物理引擎使用本地 `js/vendor/matter.min.js`（Matter.js 0.20.0）；`js/projects-ocean.js` 为停用的旧海洋方案，不再由 `js/main.js` 引用。
- 背景使用用户指定图片副本 `images/Projects/chiikawa-spring.jpg`，以 `62%` 不透明度全屏铺设；原始临时上传图片不修改。
- 球体采用与背景一致的扁平插画风：深色描边、不规则圆形、粉蓝奶油和少量绿色；鼠标靠近会施加低阻力推力，球体受左右墙和底部边界约束。
- 两个 `active` 项目仍为 `AI-powered Project Management Platform` 与 `MetaFit`，使用明显白色光圈、柔光和球内多颗星标；只有这两个球可点击，其他球是装饰性物理球。
- AI Project Management 详情不再显示无实际目标的 `View Details` 外链按钮。
- Projects 标题为较小的居中 `Project Showcase`，副标题为 `Click the glowing orbs to explore each project.`。
- 点击发光球会打开与插画统一的项目窗口：深色手绘描边、奶油纸张底、粉蓝标题栏、插画横幅、扁平标签；窗口可通过标题栏拖动，并限制在距视口边缘 8px 内。
- 移动端项目窗口宽度为视口的 92%，内容在窗口内滚动；关闭按钮固定在标题栏右侧。当前 6 个 `pending` 项目数据仍保留在 `PROJECTS` 中，但不在球场中显示为独立入口。

### Skills & Learning

- 当前基线为：`6 Skill + 2 Learning + 1 Play` 的 `3 x 3` 翻牌区，导航使用单一 `Skills & Learning` 入口指向该合并 section。
- 普通卡片点击后翻面显示文字；`AI Video Production` 翻面后先显示英文确认框，确认后打开可拖拽、可关闭的视频窗口。
- `Me & My Friends` 翻面后先显示英文确认框，确认后平滑下滑到主站第六屏的翻牌游戏，并在进入时重置牌局。
- 右侧使用用户提供的 Chiikawa 参考图副本 `images/Play/chiikawa-side.jpg`，原始外部素材不修改。
- 媒体窗口已接入由原始 MOV 转码生成的 `images/Play/ai-video-production.m4v`；原始 MOV 保留不变。

### Me & My Friends

- 游戏以内嵌 `#travel-memory` section 存在于主站，不使用独立 `play.html`。
- 使用 `images/Play/` 的 12 张照片组成 6 个编号配对，支持洗牌、匹配、错误回翻、计步、重置和完成状态。
- 游戏视觉继续使用网站的白色、淡蓝、淡粉和淡紫配色，并在桌面端保留 Chiikawa 陪伴图。

## 修改与验证

- 保持改动范围聚焦当前步骤，不顺手重构无关内容。
- 图片素材默认不改动；如需裁切、压缩或生成新素材，先说明用途并保留原文件。
- 页面样式修改后必须在桌面尺寸和窄屏尺寸检查布局，确保文字、人物、标签和交互入口互不遮挡。
- 完成验证后把验证方式和结果写入当天日志。
- 若发现 `AGENTS.md` 中的阶段描述再次落后于代码现状，应优先更新本文件或日志，再继续开发。
